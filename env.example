# LMX Consulting - Environment Variables Example
# Copy this file to .env.local and fill in your actual values

# ===========================================
# REQUIRED VARIABLES
# Development: Shows warnings but allows app to boot
# Production: Fails build/startup if missing
# ===========================================

# Database Configuration
MONGODB_URI=mongodb://localhost:27017/lmx-consulting
# Example for MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/lmx-consulting

# Authentication & Security
JWT_SECRET=your-super-secret-jwt-key-here-make-it-long-and-random

# OpenAI API Configuration
OPENAI_API_KEY=sk-your-openai-api-key-here

# ===========================================
# OPTIONAL VARIABLES (Have sensible defaults)
# ===========================================

# Database Name (defaults to 'lmx-consulting')
MONGODB_DB=lmx-consulting

# Resource Import Path (defaults to platform-specific path)
# Windows: C:/Users/[username]/OneDrive/Desktop/resources
# Linux/Mac: /resources-import
RESOURCE_IMPORT_PATH=C:/Users/jline/OneDrive/Desktop/resources

# ===========================================
# PUBLIC VARIABLES (Safe for client-side, prefixed with NEXT_PUBLIC_)
# ===========================================

# Application Branding
NEXT_PUBLIC_APP_NAME=LMX Consulting
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_DESCRIPTION=Strategic consulting and automation solutions

# ===========================================
# DEPLOYMENT NOTES
# ===========================================

# For Vercel deployment:
# 1. Go to your Vercel dashboard
# 2. Select your project
# 3. Go to Settings > Environment Variables
# 4. Add all the required variables above
# 5. Make sure to set different values for Production, Preview, and Development

# For local development:
# 1. Copy this file to .env.local
# 2. Fill in your actual values
# 3. Never commit .env.local to version control

# Security Notes:
# - Never commit .env.local or .env files with real values
# - Use strong, unique values for JWT_SECRET
# - Keep your OpenAI API key secure
# - Only variables prefixed with NEXT_PUBLIC_ are accessible on the client-side